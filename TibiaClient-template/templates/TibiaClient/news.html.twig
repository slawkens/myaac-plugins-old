<div class="windowBody">
	<div class="window innerBox">
		<div class="windowHeader">
			<img src="{{ constant('BASE_URL') }}images/news/icon_{{ icon }}.gif" alt="News Icon" class="news_image"/>{{ title }} - {{ date|date(config.news_date_format) }} - by <i>{{ author }}</i>
		</div>
		<div class="windowBody">
			{{ content|raw }}
			{% if comments is not null %}
				<div style="text-align: right; margin-right: 10px;"><a href="{{ comments }}">» Comment on this news</a></div>
			{% endif %}
		</div>
	</div>
</div>
